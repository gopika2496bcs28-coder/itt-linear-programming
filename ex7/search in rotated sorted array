bool search(int* nums, int numsSize, int target) {
    int i;
    i = 0;
    while (i < numsSize)
    {
        if (nums[i] == target)
            return (true);
        i++;
    }
    return (false);
}
